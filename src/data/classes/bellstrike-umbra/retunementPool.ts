import type { RetunementPool } from "../../../definitions/classes/classDef"

export const BELLSTRIKE_POOL: RetunementPool = {
  stats: ["affinity", "maxPhys", "momentum", "maxBellstrike", "power", "crit"],
}
