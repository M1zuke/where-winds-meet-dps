import type { GearSlot } from "../../engine/types"

export const GENERAL_SLOTS: readonly GearSlot[] = ["leftWeapon", "rightWeapon", "disc", "pendant"]
export const ARMOR_SLOTS: readonly GearSlot[] = ["helm", "armor", "greaves", "bracer"]
